import React from "react"
import { Button } from "@nextui-org/react"

export const CreateWalletButton = () => {
    return (
        <Button
            fullWidth
            variant="flat"
        >
      Create Wallet
        </Button>
    )
}
