import React from "react"
import { Button } from "@nextui-org/react"

export const ImportWalletButton = () => {
    return (
        <Button
            fullWidth
            variant="flat"
        >
      Import Wallet
        </Button>
    )
}
